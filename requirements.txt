# Web service + retrieval (Python 3.10+)
fastapi==0.135.3
uvicorn[standard]==0.44.0
pydantic==2.12.5
numpy==2.4.4
faiss-cpu==1.13.2
sentence-transformers==5.3.0
torch==2.11.0
transformers==5.5.0

# Fine-tuned models (LoRA adapters) and the cross-encoder experiment
peft>=0.19.0

# Notebooks: data preparation, evaluation, plots
pandas==3.0.2
tqdm==4.67.3
matplotlib>=3.8
nltk>=3.8
rank-bm25>=0.2.2

# Notebooks 04 and 05: synthetic query generation / hard-negative ranking via Groq
groq>=0.9
