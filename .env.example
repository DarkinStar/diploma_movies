# Copy to .env and adjust. Nothing here is required for the default setup.

# --- Retrieval (defaults shown) -------------------------------------------
# MOVIE_SEARCH_MODEL=mixedbread-ai/mxbai-embed-large-v1
# MOVIE_SEARCH_EMBEDDINGS=artifacts/embeddings_mxbai_base.npy
# MOVIE_SEARCH_METADATA=artifacts/metadata.pkl

# Serve the LoRA-fine-tuned model instead (after notebooks 05, 06 and 03):
# MOVIE_SEARCH_LORA_ADAPTER=models/lora_v2
# MOVIE_SEARCH_EMBEDDINGS=artifacts/embeddings_lora_v2.npy

# --- Optional: LLM relevance estimate shown in the UI (POST /judge) --------
# OPENROUTER_API_KEY=
# OPENROUTER_MODEL=openai/gpt-4o-mini

# --- Notebooks 04 and 05: synthetic queries and hard-negative ranking -------
# GROQ_API_KEY=
